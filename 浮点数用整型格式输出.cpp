#include <stdio.h>
#include <math.h>
#include <conio.h>
int main()
{
 double x = -2.5;
 //int xx = (int)fabs(x);
 printf("%d", x);              // 这里应该注意，如果定义的是浮点型变量，用整型格式输出的话，结果一定为0 
 getch();
return 0;
 }
 
